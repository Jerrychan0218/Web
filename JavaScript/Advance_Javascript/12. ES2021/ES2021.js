const str = "ztm is the best of the best"

str.replaceAll('best','worst'); //ztm is the worst of the worst

str.replace('best','worst'); //ztm is the worst of the best. javascript 原本就有replace，但只有針對第一個遇到的字